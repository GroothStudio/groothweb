{
  "scripts": {
    "build:main": "cd main-site && npm ci && npm run build",
    "prepare:deploy": "npm run build:main && cp -r main-site/dist/* main-site/build/",
    "build": "npm run prepare:deploy"
  }
}
